services:
  # Backend Service - Free Plan
  - type: web
    name: business-management-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install && npm run prisma:generate
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: DATABASE_URL
        fromDatabase:
          name: business-management-db
          property: connectionString

  # Frontend Service - Free Plan
  - type: web
    name: business-management-frontend
    env: node
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: npx serve -s frontend/dist -l 3000
    envVars:
      - key: VITE_API_URL
        value: https://business-management-backend.onrender.com/api

databases:
  - name: business-management-db
    databaseName: business_management
    user: bms_user
    region: oregon
    plan: free
